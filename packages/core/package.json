{
  "name": "@sublight/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Subtitle data model, validation, SRT format, cue construction — pure TS, framework-free (ADR-0001)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "pnpm typecheck",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
