{
  "name": "@sublight/protocol",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Engine API + message contracts shared by clients and engine (Spec 03)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "pnpm typecheck",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@sublight/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
